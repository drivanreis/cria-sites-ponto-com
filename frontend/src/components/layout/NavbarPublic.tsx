// src/components/layout/NavbarPublic.tsx
import React from 'react';

const NavbarPublic: React.FC = () => {
  return (
    <nav className="public-navbar">
      {/* Pode ser uma sub-navegação ou deixar vazio se não usar */}
    </nav>
  );
};

export default NavbarPublic;
